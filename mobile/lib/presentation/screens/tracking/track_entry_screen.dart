import 'package:flutter/material.dart';
class TrackEntryScreen extends StatelessWidget {
  const TrackEntryScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Track Entry')));
}