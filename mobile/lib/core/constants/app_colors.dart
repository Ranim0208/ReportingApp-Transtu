import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ─────────────────────────────────────────────────────────────────
  static const Color primary       = Color(0xFF009751); // Vert Transtu
  static const Color primaryDark   = Color(0xFF00703B);
  static const Color primaryLight  = Color(0xFFE8F5EE);
  static const Color accent        = Color(0xFFF8AF26); // Ambre ligne
  static const Color accentLight   = Color(0xFFFFF8E7);
  static const Color railBlue      = Color(0xFF0F66AD); // Bleu rail

  // ── Background ────────────────────────────────────────────────────────────
  static const Color background    = Color(0xFFE7E2D6); // Beige sable
  static const Color surface       = Color(0xFFFBF9F1); // Ticket blanc
  static const Color surfaceAlt    = Color(0xFFF1ECDD); // Ticket ombré
  static const Color border        = Color(0xFFD9D2BF); // Séparateur

  // ── Text ──────────────────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFF0E2438); // Encre foncée
  static const Color textSecondary = Color(0xFF4D5C6B); // Encre douce
  static const Color textHint      = Color(0xFF8A95A0); // Encre légère

  // ── Semantic ──────────────────────────────────────────────────────────────
  static const Color success       = Color(0xFF009751);
  static const Color successLight  = Color(0xFFE8F5EE);
  static const Color error         = Color(0xFFC0533F);
  static const Color errorLight    = Color(0xFFFBEBE8);
  static const Color warning       = Color(0xFFF8AF26);
  static const Color warningLight  = Color(0xFFFFF8E7);
  static const Color info          = Color(0xFF0F66AD);
  static const Color infoLight     = Color(0xFFE8F2FB);

  // ── Status badges ─────────────────────────────────────────────────────────
  static const Color statusNewBg         = Color(0xFFE8F2FB);
  static const Color statusNewText       = Color(0xFF0F66AD);
  static const Color statusProgressBg    = Color(0xFFFFF1D6);
  static const Color statusProgressText  = Color(0xFFA06318);
  static const Color statusResolvedBg    = Color(0xFFE8F5EE);
  static const Color statusResolvedText  = Color(0xFF00703B);
  static const Color statusClosedBg      = Color(0xFFF1ECDD);
  static const Color statusClosedText    = Color(0xFF4D5C6B);

  // ── Divider ───────────────────────────────────────────────────────────────
  static const Color divider       = Color(0xFFD9D2BF);
}